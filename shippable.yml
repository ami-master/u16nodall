language: node_js

# Version number
node_js:
  - 10.15 #.0
  - 11.6 #.0
  - 8.15 #.0
  - 6.16 #.0
  
build:
  ci:
    - node --version
    - npm --version #6.5.0
    - docker pull shippabledocker/sample_node_pvt:ecr
    
integrations:
    
  hub:
 #Docker integration
    - integrationName: ship-docker
      type: dockerRegistryLogin
